# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "2.6"
metadata:
  name: sybvensapui5.gestiondeviajes1
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: S4HANA-HTTP-100
          url: http://btptos4dcl:443
        app:
          name: ZGESTIONDEVIAJE
          description: Aplicacion de gestion de viajes
          package: ZUI5_FITV
          transport: DS4K900873
        exclude:
          - /test/
